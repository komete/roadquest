<% provide(:title, 'Routes') %>
<section>
  <div class="container">
    <div class="page-header">
      <div class="container center">
        <h3 class="session">Liste des routes</h3>
      </div>
    </div>
    <div class="row">
      <div class="col-md-6 col-md-offset-3">

        <p id="notice"><%= notice %></p>

        <table>
          <thead>
          <tr>
            <th>Num route</th>
            <th>Nombre de tronçons</th>
            <th colspan="3"></th>
          </tr>
          </thead>

          <tbody>
          <% @routes.each do |route| %>
              <tr>
                <td><%= route.num_route %></td>
                <td><%= route.troncon_routes.size %></td>
                <td><%= link_to 'Visualiser', route %></td>
              </tr>
          <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</section>

